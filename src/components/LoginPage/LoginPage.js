import React from 'react';
import LoginContainer from './LoginContainer';

const loginApp = () => (
  <div className="App">
    <header>
      <div>
        <LoginContainer />
      </div>
    </header>
  </div>
);

export default loginApp;
